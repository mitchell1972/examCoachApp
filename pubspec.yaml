name: exam_coach_app
description: A secure Flutter app for exam coaching with personalized quizzes.
publish_to: 'none' # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  logger: ^2.0.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_gherkin: ^3.0.0
  flutter_driver:
    sdk: flutter
  test: any

flutter:
  uses-material-design: true
  
  # Assets (secure asset handling)
  # assets:
  #   - assets/images/
  #   - assets/icons/ 